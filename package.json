{
  "name": "company-registration-system",
  "version": "1.0.0",
  "description": "Company Registration System with Node.js backend and React frontend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.14",
    "concurrently": "^8.2.0"
  },
  "keywords": [
    "company-registration",
    "nodejs",
    "react",
    "postgresql"
  ],
  "author": "Company Registration Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.12.2",
    "dotenv": "^17.2.3",
    "markdown-pdf": "^11.0.0",
    "pg": "^8.16.3"
  }
}
